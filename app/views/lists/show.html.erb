<%= @list.name %>
<%= link_to 'Delete', list_path(@list),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<%= link_to 'Add a bookmark', new_bookmark_path(@list.id) %>
<% @bookmarks.each do |bookmark| %>
  <%= bookmark.movie.title  %>
<% end %>
